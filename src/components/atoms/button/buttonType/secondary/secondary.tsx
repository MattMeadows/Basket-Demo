import React from "react";
import { ButtonType } from "../../type";

const secondary = ({
  children,
  disabled = false,
  onClick = () => null,
  tabIndex,
  className,
  iconLeft,
  type,
}: ButtonType) => {
  return <div>secondary</div>;
};

export default secondary;
